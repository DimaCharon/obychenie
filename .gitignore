node_modules/
npm-debug.log*

# Артефакт сборки (создаёт npm run build; на хостинге собирается заново)
dist/

# Локальные настройки приложения (ключ API!). На хостинге используйте переменные окружения.
data/
.env
.env.*

.DS_Store
